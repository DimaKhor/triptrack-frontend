import React, { useEffect, useState } from "react";
// @ts-ignore
import { observer } from "mobx-react";


const Form = observer(() => {

        return (
            <h1>hello</h1>

    );
});
export default Form;
